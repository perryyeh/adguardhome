services:
  adguardhome:
    image: adguard/adguardhome
    container_name: adguardhome
    hostname: adguardhome
    restart: always
    networks:
      macvlan:
        ipv4_address: ${adguard4}
        mac_address: ${adguardmac}
    volumes:
      - ./../adguardwork:/opt/adguardhome/work
      - ./:/opt/adguardhome/conf

networks:
  macvlan:
    external: true
    name: ${MACVLAN_NET}